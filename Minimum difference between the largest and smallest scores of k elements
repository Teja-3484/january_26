class Solution:
    def minimumDifference(self, nums: List[int], k: int) -> int:
        nums.sort()
        m = nums[k-1] - nums[0]
        for i in range(len(nums) - k + 1):
            if nums[i + k - 1] - nums[i] < m:
                m = nums[i + k - 1] - nums[i]
        return m
